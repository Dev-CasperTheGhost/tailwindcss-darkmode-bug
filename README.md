This is a repo to reproduce a bug(?) with TailwindCSS' darkmode feature.

> When using `@apply` in a css file, it will not apply the `dark:` variants. However the button styles will update since it is using `className`
